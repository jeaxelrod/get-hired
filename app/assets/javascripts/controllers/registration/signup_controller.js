"use strict";

var app = angular.module("getHired");

app.controller("SignupController", ["$scope", "$http", "$location", "FlashService",
  function($scope, $http, $location, FlashService) {

  }
]);
